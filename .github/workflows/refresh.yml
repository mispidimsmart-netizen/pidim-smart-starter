name: Refresh-Data
on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Call backend refresh
        run: |
          curl -X POST "${{ secrets.REFRESH_URL }}" || true
